
describe('<%= name %>', function() {
    check.it('accepts <%= paramTypes %>', <%= genTypes %>, function(<%= paramNames %>) {
      var ret = <%= testImportName %>.<%= name %>(<%= paramNames %>);
      <% if (PRIMITIVE_TYPES.indexOf(returnType.toLowerCase()) !== -1) { %>
        chai.assert.typeOf(ret, '<%= returnType.toLowerCase() %>');
      <% } else { %>
        chai.assert.instanceOf(ret, <%= returnType %>);
      <% } %>
    });
});
