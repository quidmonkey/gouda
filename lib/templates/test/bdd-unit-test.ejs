
describe('<%= name %>', function() {
  check.it('accepts <%= paramTypes %>', <%= genTypes %>, function(<%= paramNames %>) {
    var ret = <%= testImportName %>.<%= name %>(<%= paramNames %>);
    expect(ret).toEqual(jasmine.any(<%= returnType %>));
  });
});
