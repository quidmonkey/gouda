
test('<%= name %>: accepts <%= paramTypes %>', check(<%= genTypes %>, function(t, <%= paramNames %>) {
    var ret = <%= testImportName %>.<%= name %>(<%= paramNames %>);
    <% if (PRIMITIVE_TYPES.indexOf(returnType.toLowerCase()) !== -1) { %>
      t.true(typeof ret === '<%= returnType.toLowerCase() %>');
    <% } else { %>
      t.true(ret instanceof <%= returnType %>);
    <% } %>
}));
